server:
  port: 9000

router:
  global:
    matchers:
      - Method=GET
  routes:
    - id: static-shortcut
      service: Static=examples/resources

    - id: static-complete
      service:
        kind: Static
        args:
          directory: examples/resources
