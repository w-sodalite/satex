server:
  port: 9000

router:
  routes:
    - id: static-shortcut
      service: Static=examples/resources

    - id: static-complete
      service:
        kind: Static
        args:
          directory: examples/resources
