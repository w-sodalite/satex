server:
  port: 9000

tracing:
  max_level: debug

router:
  routes:
    - id: method-shortcut
      matchers:
        - Method=GET
      service: Static=examples/resources

    - id: method-complete
      matchers:
        - kind: Method
          args:
            methods: 
              - POST
      service: Static=examples/resources