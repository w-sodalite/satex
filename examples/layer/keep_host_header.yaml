server:
  port: 9000

tracing:
  max_level: debug

router:
  routes:
    - id: keep-host-header-shortcut
      layers:
        - KeepHostHeader
      service: Static=examples/resources

    - id: KeepHostHeader
      layers:
        - kind: KeepHostHeader
      service: Static=examples/resources