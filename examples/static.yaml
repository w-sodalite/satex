# 服务配置
server:
  # 服务端口
  port: 8000

# 路由配置
router:
  # 路由表
  routes:
    - id: backend
      matchers:
        - Path=/backend/:path+
      layers:
        - RewritePath=/{{path}}
      service: Proxy=http://127.0.0.1:3000

    # 静态文件
    - id: static
      service: Static=examples/resources